# Journal — woocommerce-on

Dated, **unverified** learnings captured the moment they're discovered — one file per finding, `symptom → cause → rule`. They ride the next PR and are promoted into `AGENTS.md › Gotchas` once re-verified against the code.

Treat entries here as leads, not yet rules. `AGENTS.md › Gotchas` is the trusted source.
