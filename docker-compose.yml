services:
  mariadb:
    container_name: dev-mariadb
    image: bitnami/mariadb:latest
    restart: unless-stopped
    environment:
      - ALLOW_EMPTY_PASSWORD=yes
      - MARIADB_USER=bn_wordpress
      - MARIADB_DATABASE=bitnami_wordpress
    networks:
      - wordpress_network
    volumes:
      - database:/bitnami/mariadb
    ports:
      - "3306:3306"

  wordpress:
    container_name: dev-wordpress
    image: bitnami/wordpress:latest
    restart: unless-stopped
    depends_on:
      - mariadb
    ports:
      - "8080:8080"
      - "8443:8443"
    environment:
      - ALLOW_EMPTY_PASSWORD=yes
      - WORDPRESS_DATABASE_HOST=mariadb
      - WORDPRESS_DATABASE_PORT_NUMBER=3306
      - WORDPRESS_DATABASE_USER=bn_wordpress
      - WORDPRESS_DATABASE_NAME=bitnami_wordpress
      - WORDPRESS_USERNAME=admin
      - WORDPRESS_PASSWORD=123456789
      - WORDPRESS_EMAIL=user@moloni.com
      - WORDPRESS_FIRST_NAME=John
      - WORDPRESS_LAST_NAME=Doe
      - WORDPRESS_BLOG_NAME=Generic Shop
    networks:
      - wordpress_network
    volumes:
      - site:/bitnami/wordpress
      - .:/bitnami/wordpress/wp-content/plugins/moloni-on

networks:
    wordpress_network:
volumes:
  site:
  database:
