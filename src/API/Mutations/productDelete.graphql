mutation productDelete($companyId: Int!,$productId: [Int]!)
{
    productDelete(companyId: $companyId,productId: $productId)
    {
        status
        deletedCount
        elementsCount
        errors
        {
            field
            msg
        }
    }
}
